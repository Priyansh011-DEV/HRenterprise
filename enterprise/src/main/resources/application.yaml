spring:
  application:
    name: enterprise
  datasource:
    url: jdbc:postgresql://db.tbucyfwmeqlnjtudjbes.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 9917107774Pri
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081